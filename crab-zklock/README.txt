创建一个永久性节点，作锁的根目录

获取锁：
    在锁根目录下创建一个临时有序列的节点（CreateMode为EPHEMERAL_SEQUENTIAL）

    获取锁根目录下所有临时节点
    if 锁根目录下没有临时节点比自己小: 成功获得锁
    else: 监听比自己小的上一个节点的删除事件，当前锁处于等待状态

    if 等待事件超过zk session连接时间，此session过期: zk自动删除此临时节点，等待状态结束，获取锁失败

    当比自己小的上一个临时节点的删除监听器触发时，等待状态结束，成功获得锁

释放锁：
    删除自己创建的临时节点