创建一个永久性节点，作锁的根目录

获取锁：
    1、在锁根目录下创建一个临时有序列的节点
    2、检查锁目录的子节点是否有序列比它小，若有则监听比它小的上一个节点的删除事件，当前锁处于等待状态
    3、当等待时间超过Zookeeper session的连接时间（sessionTimeout）时，当前session过期，Zookeeper自动删除此session创建的临时节点，等待状态结束，获取锁失败
    4、当监听器触发时，等待状态结束，获得锁

释放锁：
    删除自己创建的临时节点